# 定数

定数とは、一度値を代入したら変更ができない変数のこと。

ずっと変わらない値・変えたくない値を保持したいときに使用する。

`define("定数名", 値);`で定義し、`$`はつけない。

一般的に定数名は大文字で定義する。(慣習)

変数とはことなり、書き換え(再代入)は付加。

```
<?php
define("APP_NAME", "John's Dog App");
echo "Welcome to " . APP_NAME;
```

