# span

特に意味はないけど、ここだけデザインを変えたいというときなんかに使用するタグ。

## divとspan

- `<div>`: ブロック要素

  - 箱。書くと必ず改行される
  - レイアウト(外枠)を作るのに使う

- `<span>`: インライン要素

  - 付箋。書いても改行されない
  - 文章の一部や、アイコンだけの色を変えたい時に使う

```html
<p>
  今日の天気は<span class="text-blue-500">雨<span>です。
</p>
```

